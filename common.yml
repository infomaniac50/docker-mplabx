services:
  x11:
    # devices:
      # - /dev/dri/card0:/dev/dri/card0
    environment:
      - DISPLAY
    # privileged: true
    stdin_open: true
    tty: true
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:ro
version: '2.1'
